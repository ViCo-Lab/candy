#import "@preview/candy:0.1.0": *
#show: candy

// Feature: #transition — docs/reference/directives.md
// Mark a slide transition ("cut" between scenes). Only "cut" is fully
// implemented; "fade"/"slide" are recorded for future versions. Inert under
// standard Typst.
#scene()[
  #mobject("a", circle(radius: 1cm, fill: blue))
  #animate("a", to: (4cm, 2cm), duration: 600, easing: "smooth")
  #transition(kind: "cut", duration: 100)
  #mobject("b", rect(width: 1.5cm, height: 1.5cm, fill: red))
  #animate("b", to: (10cm, 4cm), duration: 600, easing: "smooth")
]
