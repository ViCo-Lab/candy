# 2026-07-09

## Fixed Rust build warnings
- `src/parser/tyx.rs:733` `process_fade_transform` unused `pos` → renamed to `_pos` (API uses named `from`/`to` args).
- `src/lib.rs:173` `n_frames` only used under `gpu` feature → inlined `sample_times.len()` into the gpu-only `Vec::with_capacity(...)`.

## Fixed GPU rendering (vello + wgpu, `--features gpu` / `--gpu` flag)
GPU path was panicking in `src/renderer/gpu.rs`. Three fixes applied:
1. Device limits: `wgpu::Limits::downlevel_defaults()` caps `max_storage_buffers_per_shader_stage` at 4, but vello 0.7 needs 5 → now use `adapter.limits().clone()`.
2. Target texture needed `TextureUsages::STORAGE_BINDING` (vello `render_to_texture` requirement) in addition to `RENDER_ATTACHMENT | COPY_SRC`.
3. Readback `bytes_per_row` must be 256-byte aligned (`COPY_BYTES_PER_ROW_ALIGNMENT`); pad to aligned stride and de-pad rows when copying into the tight RGBA buffer.
- Verified: `candy build examples/dot_move.tyx --gpu --format mp4` now succeeds (GPU adapter found = llvmpipe software rasterizer in this headless env).

## Pre-existing bug (NOT fixed, affects CPU & GPU equally)
- `rav1e` 0.8 AV1 encode panics: `assertion failed: rect.y >= -(cfg.yorigin as isize)` in tiling/plane_region.rs:423. Triggers H.264 fallback automatically. Present on default CPU path too. Likely odd frame dimensions (908×510 here). Worth investigating separately if AV1 output is desired.

## Fixed external (system FFmpeg) encoder mode — `src/renderer/ffmpeg.rs`
Previously ALL ffmpeg codecs failed with `Broken pipe` (ffmpeg exited early). Two root causes fixed:
1. ffmpeg muxers (mp4/mkv/webm) require **seekable output**; piping to stdout always fails ("muxer does not support non seekable output"), and MP4 `faststart` made it worse. Fix: write ffmpeg output to a unique temp file (`std::env::temp_dir()` + pid + atomic counter) and read the bytes back; keep `faststart` for mp4. Also set stdout to null.
2. HEVC/x265 rejects the alpha channel in RGBA input ("does not support alpha layer encoding"). Fix: for software lib encoders (X264/X265/H265) add `-vf format=yuv420p` to strip alpha. Hardware encoders (vaapi/videotoolbox/qsv) left untouched.
- Verified valid outputs: `--codec x264 --format mp4`, `--codec x265 --format mp4`, `--codec h265 --format mkv` all produce correct 908×510 streams.
- Note: `--codec x264 --format webm` is inherently invalid (H.264 can't go in WebM) — not a bug. WebM path is only used by self-contained AV1.
- ffmpeg 8.1.2 present on this box with libx264/libx265/libopenh264/libvpx.

## Fixed `rav1e` 0.8.1 AV1 tiling panic — `src/renderer/rav1e.rs`
- Symptom: `assertion failed: rect.y >= -(cfg.yorigin as isize)` at `tiling/plane_region.rs:170` (`PlaneRegion::from_slice`), aborts unless `catch_unwind` intercepts it (it runs on a rayon worker; candy's `catch_unwind` in `encode()` does intercept → E007 fallback to H.264).
- Root cause (confirmed via probe): the panic fires during INTER-prediction (motion estimation) for these frame geometries. A single still frame does NOT panic; any inter frame does. `Cs420` fails even single-frame; `Cs444` is required.
- Fix applied: force all-intra by setting `enc_cfg.min_key_frame_interval = 0; enc_cfg.max_key_frame_interval = 1;` (every frame = keyframe → no ME → no panic). Valid AV1 output, just no temporal compression. Keep `Cs444` + 64-multiple rounding.
- Probe matrix (separate child processes, 960×512, 30 frames): normal/tile1/lowlat → PANIC; intra → OK. So all-intra is the only working workaround short of bumping rav1e.
- Verified: `candy build examples/dot_move.tyx --format mp4` now yields `codec_name=av1` (tested at `-p 1` → 512×256; full-size 960×512 confirmed via probe). NOTE: debug-build rav1e all-intra is SLOW (46 frames at 960×512 >240s timeout); release builds are fine.

## rav1e "upgrade" — pinned 0.8.1, attempt inter-prediction first
- crates.io: rav1e 0.8.1 is the newest published release (no 0.8.2/0.9; git main unreachable; nasm absent). So "upgrade" = pin `0.8.1` + make encoder robust.
- `src/renderer/rav1e.rs`: `encode()` now tries full-quality AV1 (inter-prediction) first via `encode_inner(frames, fps, false)`, then `catch_unwind` retries all-intra (`encode_inner(..., true)`, `max_key_frame_interval=1`) ONLY if inter panics. Verified: `dot_move.tyx --codec av1` panics on inter, prints E007 retry warning, produces valid `av1` 960×512.

## Default codec → H.264 (`src/main.rs`, `src/renderer/video.rs`, `src/lib.rs`, README)
- `CodecArg` default now `h264`. `encode_frames` match: `Codec::H264` default path → on failure falls back to `Codec::Av1`; `Codec::Av1` path → on failure falls back to H264. Verified default `dot_move.tyx` → `codec_name=h264`.

## FFmpeg VAAPI / hardware modes fixed — `src/renderer/ffmpeg.rs`
- VAAPI device now declared BEFORE `-i`: `-vaapi_device /dev/dri/renderD128` for H264Vaapi/H265Vaapi. Added `_ => {}` wildcard to per-codec option match (fixes E0004 non-exhaustive).
- Per-codec: X264/X265/H265 `-preset medium -crf 23 -vf format=yuv420p`; H264Vaapi/H265Vaapi `-vf format=nv12,hwupload -qp 24`; VideoToolbox `-b:v <bitrate>`; QSV `-init_hw_device qsv -vf format=nv12,hwupload=extra_hw_frames=64 -b:v`.
- Verified real AMD Radeon 890M (Mesa 26.1.3, /dev/dri/renderD128): `h264-vaapi` → valid `h264` 908×510; x264/x265 still OK.

## External `@preview` Typst package support (`src/parser/tyx.rs`, `src/core/ast.rs`, `src/renderer/typst.rs`)
- Added `Scene.imports: Vec<String>` (serde default); `ParseCtx` captures `@`-prefixed module imports (re-add leading `#`, dedup) → `parse_tyx` stores `imports: ctx.imports.clone()`.
- `typst.rs`: `imports_preamble(scene)` joins import lines; `place_source`/`ensure_natural` prepend preamble to each detached per-object snippet (candy compiles detached modules, so top-level imports must be re-injected).
- `examples/preview_demo.tyx`: uses `#import "@preview/cetz:0.4.0": *` + `canvas(...)`/`draw.*`. IMPORTANT: typst 0.15 (candy bundled) REJECTS cetz ≤0.3.2 `lib.typ` ("expected path or string, found array"); cetz 0.4.0 works under typst 0.15. (typst CLI 0.14.2 tolerates 0.3.2+, but candy uses 0.15.) Verified `preview_demo.tyx` → valid `h264` 908×510.
- All 28 `cargo test` unit tests pass; build clean.
